Django>=4.2
djangorestframework>=3.14
sqlite3  # Built-in with Python, no need to install separately
python-dotenv>=1.0  # For managing environment variables
django-cors-headers>=4.0  # If CORS support is needed
